package springBatchDemo

class EmailService {

    public void sendMessage(customerId, message) {
        System.out.println("Sending email to customer ID " + customerId + " with message " + message);
    }
}
